{
  "name": "ddbs-cdn",
  "version": "1.0.0",
  "private": true,
  "description": "Simulated CDN with microservices (Node.js) — origin, edge caches, replication, coordinator, analytics, gateway, dashboard",
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "install:all": "npm install --workspaces --if-present",
    "dev:origin": "npm --workspace services/origin run dev",
    "test": "node --test \"tests/*.test.js\""
  }
}
